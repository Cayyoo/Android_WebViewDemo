<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.webview"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".one.One01Activity"
            android:theme="@android:style/Theme.NoTitleBar" />

        <activity
            android:name=".one.One02Activity" />

        <activity android:name=".two.TwoActivity"/>
        <activity
            android:name=".two.TwoHttpWebActivity"
            android:windowSoftInputMode="adjustUnspecified|stateHidden"/>
        <activity android:name=".two.TwoLocalWebActivity"/>
        
        <activity
            android:name=".three.ThreeActivity"
            android:label="ScrollViewCompatWebView"/>

        <activity android:name=".four.FourActivity"
            android:label="WebViewSaveCookie"/>
    </application>

</manifest>
